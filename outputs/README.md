Create:

```markdown
# Output Files

This directory contains representative outputs generated by the application.

Examples include:

- REST API responses
- Semantic search results
- RAG responses

The actual runtime database and FAISS index are excluded from Git because they are generated locally by the application
